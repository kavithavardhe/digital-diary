# Screenshots

Replace these placeholder images with actual screenshots of the running application:

1. Start the backend: `cd backend && .\mvnw.cmd spring-boot:run`
2. Start the frontend: `cd frontend && npm run dev`
3. Open http://localhost:5173 in your browser
4. Take screenshots of:
   - Login page (`login.png`)
   - Register page (`register.png`)
   - Dashboard with entries (`dashboard.png`)
   - Create/Edit entry form (`create-entry.png`)
   - Entry detail view (`detail-view.png`)
